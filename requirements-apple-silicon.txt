# Requirements for Apple Silicon Mac (M1/M2/M3)
# This file contains the exact versions tested and optimized for Apple Silicon

# Core dependencies for Apple Silicon Mac
ultralytics==8.1.0
opencv-python==4.8.1.78
tqdm==4.66.1
nats-py==2.4.0
numpy==1.24.3

# PyTorch for Apple Silicon (MPS support) - Official Apple builds
torch==2.1.2
torchvision==0.16.2

# Configuration and utilities
PyYAML==6.0.1

# Optional dependencies
pillow==10.1.0
matplotlib==3.8.2
seaborn==0.13.0

# Development dependencies
pytest==7.4.3
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# Installation notes:
# 1. Install PyTorch first: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# 2. Then install other requirements: pip install -r requirements-apple-silicon.txt
# 3. PyTorch will automatically use MPS when available on Apple Silicon
